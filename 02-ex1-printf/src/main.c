#include <stdio.h>
// This is a preprocessor statement/directive marked by a # that is including a library.
// stdio.h is a standard C library. printf is found in stdio.h

int main() // main() is the function. The term main has a special meaning in C. Every program should only have ONE main.
{
	printf("my name is danny\n");
	return 0;
}
